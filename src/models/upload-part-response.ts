/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type UploadPartResponse = {
  /**
   * Presigned URL to upload the part.
   */
  url: string;
  /**
   * Unix timestamp.
   */
  expiresAt: number;
};

/** @internal */
export const UploadPartResponse$inboundSchema: z.ZodMiniType<
  UploadPartResponse,
  unknown
> = z.pipe(
  z.object({
    url: types.string(),
    expires_at: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "expires_at": "expiresAt",
    });
  }),
);

export function uploadPartResponseFromJSON(
  jsonString: string,
): SafeParseResult<UploadPartResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UploadPartResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UploadPartResponse' from JSON`,
  );
}
