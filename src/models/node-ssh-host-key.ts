/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type NodeSshHostKey = {
  /**
   * Key algorithm.
   */
  keyType: string;
  /**
   * Base64-encoded public key.
   */
  key: string;
};

/** @internal */
export const NodeSshHostKey$inboundSchema: z.ZodMiniType<
  NodeSshHostKey,
  unknown
> = z.pipe(
  z.object({
    key_type: types.string(),
    key: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "key_type": "keyType",
    });
  }),
);

export function nodeSshHostKeyFromJSON(
  jsonString: string,
): SafeParseResult<NodeSshHostKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NodeSshHostKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NodeSshHostKey' from JSON`,
  );
}
